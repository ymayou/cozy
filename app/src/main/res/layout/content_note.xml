<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".NoteActivity"
    tools:showIn="@layout/activity_note">

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/noteTitle"
        android:hint="Title"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="60dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/noteContent"
        android:layout_below="@+id/noteTitle"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/noteTitle"
        android:hint="Note"
        android:layout_above="@+id/noteButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Validate"
        android:id="@+id/noteButton"
        android:layout_marginBottom="51dp"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/noteContent" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:id="@+id/noteCancel"
        android:layout_alignTop="@+id/noteButton"
        android:layout_toStartOf="@+id/noteButton" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Color"
        android:id="@+id/noteColor"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/noteCancel" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Text size"
        android:id="@+id/noteSize"
        android:layout_alignTop="@+id/noteColor"
        android:layout_toStartOf="@+id/noteColor" />

    <RadioButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bold"
        android:id="@+id/noteBold"
        android:checked="false"
        android:layout_alignBottom="@+id/noteSize"
        android:layout_toStartOf="@+id/noteSize"
        android:layout_marginEnd="39dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:id="@+id/noteDelete"
        android:layout_alignBottom="@+id/noteCancel"
        android:layout_alignParentStart="true"
        android:onClick="deleteNote" />
</RelativeLayout>
